# Google Cloud Secret Manager Configuration (Non-Secret metadata)
# In production, the backend server IAM identity uses these to retrieve the secret from Secret Manager:
GCP_PROJECT_ID="ai-studio-securegeminijour-2dffa76d-6c49-49ad-863c-fc45cef0f4a7"
GEMINI_SECRET_NAME="gemini-api-key"
GEMINI_SECRET_VERSION="latest"

# Local Development / Sandbox fallback only (Never commit actual secret values):
GEMINI_API_KEY="your_gemini_api_key_here"

# Application Host URL
APP_URL=""
